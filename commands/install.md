---
description: Install jikko commands to ~/.claude/commands
---
```bash
jikko install $ARGUMENTS
```

Options:
- `--force` / `-f` - Overwrite existing commands directory
- `--copy` / `-c` - Copy instead of symlink
